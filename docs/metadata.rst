Metadata
========

The :func:`stdlib_list` function grabs the stored data in CSV files (as part of this module) and uses the non-version arguments to determine a final list of libraries to return. This helper function grabs the metadata used by :func:`stdlib_list`.

.. automodule:: stdlib_list
    :members: metadata
